# MCP Container Sandbox Server - 자동 배포 워크플로우
#
# 이 워크플로우는 main 브랜치에 푸시될 때 자동으로 Hostinger VPS에 배포합니다.
#
# 필요한 GitHub Secrets:
#   - VPS_HOST: VPS IP 주소 또는 도메인 (예: playmcp-sandbox-computer.bloupla.net)
#   - VPS_USER: SSH 사용자명 (보통 root)
#   - VPS_SSH_KEY: SSH 개인키 (id_rsa 또는 id_ed25519 내용)
#   - VPS_PORT: SSH 포트 (선택사항, 기본값 22)
#
# Secrets 설정 방법:
#   1. GitHub 저장소 → Settings → Secrets and variables → Actions
#   2. "New repository secret" 클릭
#   3. 위 Secrets들을 하나씩 추가

name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 수동 실행 허용

env:
  APP_DIR: /opt/mcp-container-server
  SERVICE_NAME: mcp-container

jobs:
  deploy:
    name: Deploy to Hostinger VPS
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.VPS_PORT || 22 }} -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy to VPS
        env:
          SSH_HOST: ${{ secrets.VPS_HOST }}
          SSH_USER: ${{ secrets.VPS_USER }}
          SSH_PORT: ${{ secrets.VPS_PORT || 22 }}
        run: |
          ssh -i ~/.ssh/deploy_key -p $SSH_PORT $SSH_USER@$SSH_HOST << 'DEPLOY_SCRIPT'
            set -e
            
            echo "========================================="
            echo "🚀 MCP Container Server 배포 시작"
            echo "========================================="
            
            APP_DIR="/opt/mcp-container-server"
            SERVICE_NAME="mcp-container"
            BACKUP_DIR="${APP_DIR}-backup-$(date +%Y%m%d%H%M%S)"
            
            # 백업 생성
            if [ -d "$APP_DIR" ]; then
              echo "📦 기존 버전 백업 중..."
              cp -r "$APP_DIR" "$BACKUP_DIR"
            fi
            
            cd "$APP_DIR"
            
            # Git pull
            echo "📥 최신 코드 가져오기..."
            git fetch origin main
            git reset --hard origin/main
            
            # 의존성 설치 및 빌드
            echo "📦 의존성 설치 중..."
            # package-lock.json이 없으면 생성
            if [ ! -f "package-lock.json" ]; then
              echo "📝 package-lock.json 생성 중..."
              npm install --package-lock-only
            fi
            npm install
            
            echo "🔨 빌드 중..."
            npm run build
            
            # 프로덕션 의존성만 유지
            npm prune --omit=dev
            
            # 서비스 재시작 (서비스가 없으면 설치)
            echo "🔄 서비스 재시작 중..."
            if ! systemctl is-enabled $SERVICE_NAME &>/dev/null; then
              echo "🛠️ systemd 서비스 설치 중..."
              
              # 환경변수 파일 생성
              echo "PORT=3000" > "$APP_DIR/.env"
              echo "BASE_URL=https://playmcp-sandbox-computer.bloupla.net" >> "$APP_DIR/.env"
              echo "NODE_ENV=production" >> "$APP_DIR/.env"
              chmod 600 "$APP_DIR/.env"
              
              # systemd 서비스 파일 생성
              echo "[Unit]" > /etc/systemd/system/${SERVICE_NAME}.service
              echo "Description=MCP Container Sandbox Server" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "After=network.target" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "[Service]" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "Type=simple" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "User=root" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "WorkingDirectory=/opt/mcp-container-server" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "EnvironmentFile=/opt/mcp-container-server/.env" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "ExecStart=/usr/bin/node /opt/mcp-container-server/dist/index.js" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "Restart=always" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "RestartSec=10" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "StandardOutput=journal" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "StandardError=journal" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "SyslogIdentifier=mcp-container" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "[Install]" >> /etc/systemd/system/${SERVICE_NAME}.service
              echo "WantedBy=multi-user.target" >> /etc/systemd/system/${SERVICE_NAME}.service
              
              systemctl daemon-reload
              systemctl enable $SERVICE_NAME
            fi
            
            systemctl restart $SERVICE_NAME
            
            # Podman 설치 및 설정 (아직 안 되어 있으면)
            if ! command -v podman &>/dev/null; then
              echo "📦 Podman 설치 중..."
              apt-get update -qq
              apt-get install -y -qq podman
            fi
            
            # Podman 스토리지 설정 (runroot 오류 해결)
            echo "🗃️ Podman 스토리지 설정 중..."
            mkdir -p /etc/containers
            mkdir -p /var/lib/containers/storage
            mkdir -p /run/containers/storage
            
            echo "[storage]" > /etc/containers/storage.conf
            echo 'driver = "vfs"' >> /etc/containers/storage.conf
            echo 'graphroot = "/var/lib/containers/storage"' >> /etc/containers/storage.conf
            echo 'runroot = "/run/containers/storage"' >> /etc/containers/storage.conf
            
            # Alpine 이미지 미리 다운로드 (최초 컨테이너 생성 속도 향상)
            echo "📥 Alpine 이미지 다운로드 중..."
            podman pull docker.io/library/alpine:latest 2>/dev/null || true
            
            # Nginx + HTTPS 설정 (아직 안 되어 있으면)
            DOMAIN="playmcp-sandbox-computer.bloupla.net"
            if ! command -v nginx &>/dev/null; then
              echo "🔐 Nginx + HTTPS 설치 중..."
              
              # Nginx 및 Certbot 설치
              apt-get update -qq
              apt-get install -y -qq nginx certbot python3-certbot-nginx
              
              # Nginx 설정 파일 생성
              echo "server {" > /etc/nginx/sites-available/mcp-container
              echo "    listen 80;" >> /etc/nginx/sites-available/mcp-container
              echo "    server_name $DOMAIN;" >> /etc/nginx/sites-available/mcp-container
              echo "    location / {" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_pass http://127.0.0.1:3000;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_http_version 1.1;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_set_header Upgrade \$http_upgrade;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_set_header Connection 'upgrade';" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_set_header Host \$host;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_set_header X-Real-IP \$remote_addr;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_set_header X-Forwarded-Proto \$scheme;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_read_timeout 86400;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_send_timeout 86400;" >> /etc/nginx/sites-available/mcp-container
              echo "        proxy_buffering off;" >> /etc/nginx/sites-available/mcp-container
              echo "    }" >> /etc/nginx/sites-available/mcp-container
              echo "}" >> /etc/nginx/sites-available/mcp-container
              
              # 사이트 활성화
              ln -sf /etc/nginx/sites-available/mcp-container /etc/nginx/sites-enabled/
              rm -f /etc/nginx/sites-enabled/default
              
              # Nginx 시작
              systemctl enable nginx
              systemctl restart nginx
              
              # SSL 인증서 발급 (Let's Encrypt)
              echo "📜 SSL 인증서 발급 중..."
              certbot --nginx -d $DOMAIN --non-interactive --agree-tos --email admin@$DOMAIN --redirect || echo "⚠️ SSL 발급 실패 - 나중에 수동으로 실행하세요"
              
              # 방화벽 설정
              ufw allow 'Nginx Full' 2>/dev/null || true
              
              echo "✅ Nginx + HTTPS 설정 완료!"
            fi
            
            # 헬스체크 대기
            echo "⏳ 서버 시작 대기 중..."
            sleep 3
            
            # 헬스체크 검증
            MAX_ATTEMPTS=10
            ATTEMPT=1
            while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
              if curl -s http://localhost:3000/health | grep -q '"status":"ok"'; then
                echo "✅ 헬스체크 통과!"
                break
              fi
              echo "대기 중... ($ATTEMPT/$MAX_ATTEMPTS)"
              sleep 2
              ATTEMPT=$((ATTEMPT + 1))
            done
            
            if [ $ATTEMPT -gt $MAX_ATTEMPTS ]; then
              echo "❌ 헬스체크 실패! 롤백 중..."
              if [ -d "$BACKUP_DIR" ]; then
                rm -rf "$APP_DIR"
                mv "$BACKUP_DIR" "$APP_DIR"
                systemctl restart $SERVICE_NAME
              fi
              exit 1
            fi
            
            # 백업 정리 (성공 시 최근 3개만 유지)
            echo "🧹 오래된 백업 정리 중..."
            ls -dt /opt/mcp-container-server-backup-* 2>/dev/null | tail -n +4 | xargs rm -rf 2>/dev/null || true
            
            echo "========================================="
            echo "✅ 배포 완료!"
            echo "========================================="
          DEPLOY_SCRIPT

      - name: Verify deployment
        run: |
          echo "🔍 외부에서 헬스체크 검증 중..."
          sleep 5
          
          # VPS_HOST를 사용하여 헬스체크 URL 구성
          HEALTH_URL="https://${{ secrets.VPS_HOST }}/health"
          echo "헬스체크 URL: $HEALTH_URL"
          
          MAX_ATTEMPTS=5
          ATTEMPT=1
          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$HEALTH_URL" 2>/dev/null || echo "000")
            if [ "$HTTP_STATUS" = "200" ]; then
              echo "✅ 외부 헬스체크 통과! (HTTP $HTTP_STATUS)"
              exit 0
            fi
            echo "대기 중... (HTTP $HTTP_STATUS, $ATTEMPT/$MAX_ATTEMPTS)"
            sleep 3
            ATTEMPT=$((ATTEMPT + 1))
          done
          
          echo "⚠️ 외부 헬스체크 타임아웃 (서버 내부에서는 정상일 수 있음)"
          # 경고만 표시하고 실패로 처리하지 않음 (DNS 전파 등의 이유로)

      - name: Cleanup SSH
        if: always()
        run: rm -f ~/.ssh/deploy_key

  notify-failure:
    name: Notify on Failure
    runs-on: ubuntu-latest
    needs: deploy
    if: failure()
    
    steps:
      - name: Create failure summary
        run: |
          echo "## ❌ 배포 실패" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "배포가 실패했습니다. 로그를 확인해주세요." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**커밋:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**브랜치:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**시간:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
